# AWG15 Config Generator

Простой генератор конфигураций AWG15 (Amnezia WireGuard 1.5) для WARP.

## 🔒 Защита от ботов (CAPTCHA)

Проект использует **Cloudflare Turnstile** для защиты от автоматических запросов. При каждом нажатии на кнопку "Получить конфигурацию" пользователю необходимо пройти проверку.

### Настройка CAPTCHA:

1. Зарегистрируйтесь в [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Перейдите в раздел **Turnstile** → **Add widget**
3. Создайте виджет типа **Managed** (рекомендуется) или **Invisible**
4. Добавьте домен вашего сайта
5. Скопируйте **Site Key** и **Secret Key**

### Установка ключей:

**Site Key** (публичный ключ) уже настроен в [`index.html`](index.html:425):
```html
data-sitekey="0x4AAAAAAA1zRXZmsyXOdHxI"
```

**Secret Key** (секретный ключ) устанавливается через переменную окружения:
```bash
# Для Vercel
vercel env add TURNSTILE_SECRET_KEY

# Или в .env файл (для локальной разработки)
TURNSTILE_SECRET_KEY=0x4AAAAAAA...
```

> **Примечание:** Если `TURNSTILE_SECRET_KEY` не установлен, проверка CAPTCHA будет пропущена (режим разработки).

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск локального сервера
npm run dev
```

### Деплой на Vercel

```bash
# Установка Vercel CLI (если не установлен)
npm i -g vercel

# Деплой
vercel

# Или деплой в продакшен
vercel --prod
```

## 📁 Структура проекта

```
├── api/
│   └── generate.js          # API endpoint для генерации конфига
├── index.html               # Простой UI
├── package.json             # Зависимости
├── vercel.json              # Конфигурация Vercel
└── README.md                # Документация
```

## 🔧 Параметры конфигурации

По умолчанию генерируется конфигурация с следующими параметрами:

| Параметр | Значение |
|----------|----------|
| Формат | AWG15 (Amnezia WireGuard 1.5) |
| Jc | 120 |
| Jmin | 23 |
| Jmax | 911 |
| H1-H4 | 1, 2, 3, 4 |
| S1-S2 | 0, 0 |
| Endpoint | 162.159.195.1:500 |
| DNS | 1.1.1.1, 2606:4700:4700::1111, 1.0.0.1, 2606:4700:4700::1001 |
| AllowedIPs | 0.0.0.0/0, ::/0 |

## 🌐 Использование

1. Откройте сайт
2. Нажмите кнопку "Сгенерировать конфиг"
3. Скопируйте или скачайте конфигурацию
4. Импортируйте в Amnezia VPN или другой клиент с поддержкой AWG15

## 📄 Лицензия

MIT License
